

principal(){

	variable entero ejemploEntero = 3;
	variable real ejemploReal = 4.5;
	variable caracter ejemploCaracter = 'f';

	variable booleano ejemploBoolea = verdadero;

	variable listade entero ejemploLista = [4,2,3,5,3,6,3];
	variable entero devuelto = ?ejemploLista;

	entero potencia(entero base, entero exponente){

		variable entero almacen = base;
		variable entero iter = 0;

		repetir{
			almacen = almacen * almacen;
			iter = iter + 1;
		}mientras(iter < exponente);

		devuelve almacen;

	}


	ejemploLista>>;

	si(ejemploEntero == devuelto || ejemploReal < 5.0){
		ejemploBooleano = falso;
	}
	sino{
		ejemploLista>>;
	}


}
